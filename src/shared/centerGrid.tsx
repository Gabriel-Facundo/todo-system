import { MainGrid } from 'style/styledsComponents'

const CenterGrid = ({children}: any) => {
    return (
        <MainGrid >
            {children}
        </MainGrid>
    )
}

export default CenterGrid